/* Basic reset & variables */
:root{
  --bg:#050505;
  --panel:#0c0c0c;
  --teal:#00f0c6;
  --muted:#9aa3a3;
  --glass: rgba(255,255,255,0.03);
  --accent-glow: 0 6px 24px rgba(0,240,198,0.12), 0 2px 6px rgba(0,240,198,0.08);
  --container-max:1200px;
  --radius:12px;
  --transition: 300ms cubic-bezier(.2,.9,.3,1);
}

*{box-sizing:border-box}
html,body{height:100%}
body{
  margin:0;
  font-family: 'Montserrat',system-ui,Segoe UI,Roboto,'Helvetica Neue',Arial;
  background:linear-gradient(180deg,#000 0%,#020202 100%);
  color:#e6f7f2;
  -webkit-font-smoothing:antialiased;
  -moz-osx-font-smoothing:grayscale;
  line-height:1.45;
}

/* containers */
.container{max-width:var(--container-max);margin:0 auto;padding:0 20px}

/* header */
header{position:fixed;top:18px;left:0;right:0;z-index:60;pointer-events:none}
.header-inner{display:flex;align-items:center;justify-content:space-between;background:transparent;padding:8px 0;pointer-events:auto}
.brand{display:flex;align-items:baseline;gap:10px}
.logo-glow{font-weight:900;font-size:22px;color:var(--teal);text-shadow:0 4px 28px rgba(0,240,198,0.14);letter-spacing:1px}
.brand-text{font-weight:500;color:#cdeee6;opacity:0.95}

/* nav */
.main-nav{display:flex;gap:18px;align-items:center}
.main-nav a{color:#cfeee8;text-decoration:none;padding:8px 12px;border-radius:8px;transition:var(--transition);font-weight:600}
.main-nav a:hover{background:rgba(255,255,255,0.02);box-shadow:var(--accent-glow)}

/* mobile */
.mobile-menu-btn{display:none;background:transparent;border:0;cursor:pointer;padding:10px}
.mobile-menu-btn span{display:block;width:22px;height:2px;background:#8ff8df;margin:4px 0;border-radius:2px;transition:all 180ms}

/* splash */
#splash{
  position:fixed;inset:0;z-index:200;display:flex;align-items:center;justify-content:center;
  background:linear-gradient(180deg, rgba(0,0,0,0.75), rgba(0,0,0,0.9));
}
.splash-content{text-align:center}
.splash-vip{
  font-family:'Playfair Display',serif;font-size:48px;color:transparent;
  -webkit-text-stroke:1px rgba(0,240,198,0.12);
  animation:vipReveal 2.2s ease forwards;
  letter-spacing:2px;margin:0 0 10px;
  text-shadow:0 10px 40px rgba(0,240,198,0.06);
}
.loader-sub{font-weight:700;color:var(--teal);opacity:0;transform:translateY(8px);animation:subIn 2.6s ease forwards 0.8s}
@keyframes vipReveal{
  0%{opacity:0;transform:translateY(-8px) scale(0.98)}
  40%{opacity:1;transform:translateY(0) scale(1.02)}
  100%{opacity:1;transform:translateY(0) scale(1)}
}
@keyframes subIn{
  to{opacity:1;transform:translateY(0)}
}

/* hide header until splash finished */
header.hidden{opacity:0;transform:translateY(-16px);transition:all 400ms}
header{transition:all 400ms}

/* HERO */
.hero-section{min-height:100vh;position:relative;display:flex;align-items:center;justify-content:center;padding:0;margin-top:48px}
.hero-bg{
  position:absolute;inset:0;background:
    linear-gradient(180deg, rgba(0,0,0,0.6), rgba(0,0,0,0.7)),
    url('https://images.unsplash.com/photo-1519125323398-675f0ddb6308?q=80&w=1600&auto=format&fit=crop&ixlib=rb-4.0.3&s=placeholder') center/cover no-repeat;
  filter:contrast(1.05) saturate(0.95) brightness(0.75);
  z-index:0;
  transform:scale(1.02);
  will-change:transform;
  animation:slowZoom 18s linear infinite alternate;
}
@keyframes slowZoom{from{transform:scale(1.02)} to{transform:scale(1.06)}}

/* film grain overlay */
.hero-bg::after{
  content:"";position:absolute;inset:0;pointer-events:none;
  background-image:linear-gradient(transparent 0%, rgba(0,0,0,0.1) 100%), url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="160" height="160"><filter id="g"><feTurbulence baseFrequency="0.9" numOctaves="2" stitchTiles="stitch"/><feColorMatrix type="saturate" values="0"/></filter><rect width="100%" height="100%" filter="url(%23g)" opacity="0.03"/></svg>');
  mix-blend-mode:overlay;
}

/* hero content */
.hero-inner{position:relative;z-index:3;text-align:center;padding:120px 16px 80px}
.brand-line{font-family:'Playfair Display',serif;font-size:44px;margin:0;color:#fff;letter-spacing:1px;text-shadow:0 14px 36px rgba(0,0,0,0.6)}
.brand-line span{color:var(--teal);text-shadow:0 8px 28px rgba(0,240,198,0.08)}
.subtitle{color:#bfeee4;font-weight:500;margin:12px 0 22px}
.hero-cta .btn{margin:6px}

/* buttons */
.btn{display:inline-block;padding:10px 18px;border-radius:10px;text-decoration:none;font-weight:700;transition:all 180ms}
.btn-primary{background:linear-gradient(90deg,var(--teal),#00d2b0);color:#021010;box-shadow:var(--accent-glow)}
.btn-outline{border:1px solid rgba(255,255,255,0.06);color:#cfeee8;background:transparent}

/* sections */
.section{padding:68px 0}
.section-title{font-family:'Playfair Display',serif;font-size:28px;margin:0 0 6px;color:#dff8f0}
.section-sub{color:var(--muted);margin:0 0 22px}

/* services */
.services-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:18px}
.service-card{background:linear-gradient(180deg,rgba(255,255,255,0.02),rgba(255,255,255,0.01));padding:18px;border-radius:16px;box-shadow:0 6px 18px rgba(2,8,12,0.6);text-align:center;border:1px solid rgba(255,255,255,0.02);transition:transform 220ms}
.service-card .icon{font-size:20px;color:var(--teal);display:block;margin-bottom:10px}
.service-card h4{margin:0;font-size:16px}
.service-card:hover{transform:translateY(-6px);box-shadow:var(--accent-glow)}

/* gallery */
.upload-area{display:flex;gap:12px;align-items:center;margin-bottom:14px;flex-wrap:wrap}
.upload-btn{display:inline-flex;align-items:center;gap:8px;padding:10px 14px;border-radius:12px;background:rgba(255,255,255,0.02);cursor:pointer}
.upload-btn input{display:none}
.muted{color:var(--muted)}

.gallery-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(210px,1fr));gap:12px}
.gallery-item{background:var(--panel);border-radius:10px;overflow:hidden;position:relative;cursor:pointer;border:1px solid rgba(255,255,255,0.02)}
.gallery-item img, .gallery-item video{width:100%;height:100%;object-fit:cover;display:block}
.gallery-item video{height:220px}
.gallery-item::after{content:"";position:absolute;inset:0;background:linear-gradient(180deg,transparent,rgba(0,0,0,0.18));pointer-events:none}

/* modal slider */
.media-modal{position:fixed;inset:0;background:rgba(1,1,1,0.85);display:none;align-items:center;justify-content:center;z-index:120;padding:40px}
.media-modal.show{display:flex}
.modal-stage{max-width:1100px;width:100%;max-height:80vh;display:flex;align-items:center;justify-content:center}
.modal-stage img, .modal-stage video{max-width:100%;max-height:80vh;object-fit:contain;border-radius:8px}
.modal-close{position:absolute;top:18px;right:22px;background:transparent;border:0;color:#fff;font-size:32px;cursor:pointer}
.modal-nav{position:absolute;top:50%;transform:translateY(-50%);background:rgba(0,0,0,0.35);border:0;color:#fff;padding:12px;border-radius:50%;cursor:pointer}
.modal-nav.prev{left:18px}
.modal-nav.next{right:18px}

/* about */
.about-inner{display:grid;grid-template-columns:260px 1fr;gap:20px;align-items:center}
.about-photo img{width:100%;border-radius:12px;display:block}
.about-bio h4{margin:0 0 6px;font-size:18px}
.about-bio p{color:#d7f7ef}

/* contact */
.contact-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:18px}
.contact-card{background:var(--panel);padding:18px;border-radius:12px;border:1px solid rgba(255,255,255,0.02)}
.contact-card a{color:var(--teal);text-decoration:none}

/* whatsapp button */
.whatsapp-btn{position:fixed;right:18px;bottom:18px;background:linear-gradient(180deg,var(--teal),#00c39d);color:#021010;padding:10px 12px;border-radius:999px;display:flex;align-items:center;gap:8px;text-decoration:none;box-shadow:var(--accent-glow);z-index:140}
.whatsapp-btn i{font-size:18px}
.whatsapp-btn span{font-weight:700}

/* footer */
.site-footer{border-top:1px solid rgba(255,255,255,0.03);padding:14px 0;margin-top:28px;background:transparent}
.footer-inner{display:flex;justify-content:space-between;align-items:center}
.socials a{color:#cfeee8;margin-left:12px}

/* responsive */
@media (max-width:900px){
  .about-inner{grid-template-columns:1fr}
  .hero-inner{padding:80px 16px}
  .mobile-menu-btn{display:block}
  .main-nav{display:none}
  header{top:12px}
  .section{padding:48px 0}
  .logo-glow{font-size:20px}
  .brand-line{font-size:36px}
  .splash-vip{font-size:36px}
  .gallery-item video{height:160px}
  .hero-bg{animation:slowZoom 26s linear infinite alternate}
}

/* accessible sr-only for forms */
.sr-only{position:absolute!important;height:1px;width:1px;overflow:hidden;clip:rect(1px,1px,1px,1px);white-space:nowrap}